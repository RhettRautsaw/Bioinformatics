# ZSH profile

COMPNAME="Rhett@iMac"
export ZSH=~/Dropbox/bin/ohmyzsh
ZSH_THEME="Rhett"
plugins=(git)

source $ZSH/oh-my-zsh.sh
source $ZSH/zsh-git-prompt/zshrc.sh

export R_MAX_VSIZE=32000000000

# GLOBAL GIT IGNORE
#echo ".DS_Store" >> ~/.gitignore_global
#echo "._.DS_Store" >> ~/.gitignore_global
#echo "**/.DS_Store" >> ~/.gitignore_global
#echo "**/._.DS_Store" >> ~/.gitignore_global
#echo ".Rhistory" >> ~/.gitignore_global
#echo "**/.Rhistory" >> ~/.gitignore_global
#git config --global core.excludesfile ~/.gitignore_global

# SSH Profiles
alias palmetto="caffeinate ssh rrautsa@login.palmetto.clemson.edu"
alias droplet="caffeinate ssh root@reptilerhett-droplet-1"

# Add the following lines to ~/.ssh/config to allow multiple ssh logins 
# without retyping password every time. Quick SSH!
# ControlMaster auto
# ControlPath ~/.ssh/control:%h:%p:%r

# Alias Functions
alias printline="eval printf %.0s* '{1..'"${COLUMNS:-$(tput cols)}"\}; echo"
alias ll="ls -lh"
alias d="conda deactivate"
alias qgis="conda activate qgis_env; qgis"
alias bio="conda activate bio"
alias envs="conda info --envs"
alias tree="tree --du -h"
alias diff2="diff -y -W $(tput cols)"

# Functions
function xman() {
	open x-man-page://$1 ;
}

# PATH
export PATH=/usr/local/bin:/usr/bin:/usr/sbin:/bin:/sbin
export PATH=$PATH:~/Dropbox/bin:~/Dropbox/bin/Scripts
export PATH=$PATH:~/Dropbox/bin/RokytaScripts
export PATH=$PATH:~/Dropbox/bin/AndrewScripts
export PATH=$PATH:~/Dropbox/GitHub/Bioinformatics/scripts
export PATH=$PATH:~/Dropbox/bin/trinityrnaseq-Trinity-v2.8.5
export PATH=$PATH:~/Dropbox/GitHub/BarcodeIDChecker
export PATH=$PATH:~/Dropbox/bin/raxml-ng
export PATH=$PATH:~/Dropbox/bin/ProbeSets/SquamateConservedLoci
export PATH=$PATH:~/Dropbox/bin/Astral
export PATH=$PATH:~/Dropbox/bin/PAUP
export PATH=$PATH:~/Dropbox/bin/RevBayes_OSX_v1.0.13
export PATH=$PATH:~/Dropbox/bin/fsc26_mac64
export PATH=$PATH:~/Dropbox/bin/HyDe/scripts
export PATH=$PATH:~/Dropbox/bin/MAPS/src
export PATH=$PATH:~/Dropbox/bin/eems/runeems_snps/src
export PATH=$PATH:~/Dropbox/bin/eems/bed2diffs/src-wout-openmp
export PATH=$PATH:~/Dropbox/bin/Venomancer/bin
export PATH=$PATH:~/Dropbox/bin/CodAn/bin

# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/opt/anaconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/opt/anaconda3/etc/profile.d/conda.sh" ]; then
        . "/opt/anaconda3/etc/profile.d/conda.sh"
    else
        export PATH="/opt/anaconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
